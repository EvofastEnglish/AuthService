apiVersion: v1
kind: Service
metadata:
  name: evofast-authservice-service
spec:
  selector:
    app: evofast-authservice
  ports:
  - protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
